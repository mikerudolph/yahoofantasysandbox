var express = require('express');
var app = express();
var router = express.Router();

var controller = require('../controllers/data');

var data = function() {

    return router;

}();

module.exports = data;
